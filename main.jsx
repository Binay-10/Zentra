import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

import './style.css'; // Make sure to move your original style.css into the src/ folder!

ReactDOM.createRoot(document.getElementById('root')).render(
    <App />
);